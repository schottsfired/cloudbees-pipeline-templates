version: 1
type: pipeline-template
templateType: multibranch

name: MultiBranch Pipeline Template
description: MultiBranch Pipeline Template
parameters:
  - name: repoName
    type: string
    displayName: Which repo are we building? #try: schottsfired/multibranch-demo
multibranch:
  branchSource:
    github:
      repoOwner: schottsfired
      repository: ${repoName}
      #scanCredentialsId: my-team-github-credentials